# --- App mode ---
MODE=paper

# --- API keys (empty for paper mode) ---
MEXC_API_KEY=mx0vglMqAeHTfRZPYR
MEXC_API_SECRET=0dcae79f3a0f4cc29378b74a98b88d45

UI_ID=WEB1492f7dc210631f57ac5dca5e17c3f3516142645aeebdeba4c17976a150421ec


# --- Frontend CORS ---
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000


# --- Symbols (comma or semicolon separated) ---
SYMBOLS=ATHUSDT,HBARUSDT

# --- REST fallback (unused when WS is on, keep for completeness) ---
REST_BASE_URL=https://api.mexc.com/api/v3
POLL_INTERVAL_SEC=1.5
DEPTH_LIMIT=20

# --- WebSocket endpoint & timeouts ---
WS_URL_PUBLIC=wss://wbs-api.mexc.com/ws
WS_OPEN_TIMEOUT=20
WS_CLOSE_TIMEOUT=5

# --- Data source toggles ---
ENABLE_WS=true
ENABLE_PS_POLLER=false

WS_URL_PUBLIC=wss://wbs-api.mexc.com/ws

WS_VERBOSE_FRAMES=false
WS_VERBOSE_HEXDUMP=false
WS_ENABLE_BRUTEFORCE=false
WS_LOG_THROTTLE_MS=1000

WS_DEBUG_JSON_PARITY=false
WS_DEBUG_PB_VARIANTS=false

ENABLE_UI_STATE=true
WORKSPACE_ID=1
UI_REVISION_SEED=1
MAX_WATCHLIST_BULK=50
IDEMPOTENCY_WINDOW_SEC=30
SSE_RETRY_BASE_MS=1000
SSE_PING_INTERVAL_MS=5000

DATABASE_URL=sqlite:///C:/projects/mexc-trade-bot/backend/mexc.db


# --- Optional DNS override (uncomment ONLY if local DNS is flaky) ---
# WS_DNS_OVERRIDE=108.138.26.8
# WS_SERVER_HOSTNAME=wbs-api.mexc.com
# WS_FORCE_OVERRIDE=true

# --- Optional proxy envs (ONLY if you must use a corporate proxy) ---
# HTTPS_PROXY=http://user:pass@proxy.example.com:8080
# HTTP_PROXY=http://user:pass@proxy.example.com:8080
# NO_PROXY=localhost,127.0.0.1
