# ML Data Collector Configuration
SYMBOLS=LINKUSDT,NEARUSDT,VETUSDT,AVAXUSDT,ALGOUSDT

# Database — local PostgreSQL (same instance as backend/researcher).
# Take the value from backend/.env (DATABASE_URL).
DATABASE_URL=postgresql://mexc:PASSWORD@127.0.0.1:5432/trading_bot

MEXC_WS_URL=wss://wbs.mexc.com/ws
SCANNER_BASE_URL=http://127.0.0.1:8000/api/scanner/mexc/top
COLLECTION_INTERVAL=10
LOG_LEVEL=INFO
